# Sanity — must match the values used in ../bvilhavo-studio
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-10-01

# OpenWeather — live temperatura/humidade + risco de incêndio na barra de topo.
# Chave gratuita em https://openweathermap.org/api (Current Weather + Forecast).
# Só é lida no servidor (rota /api/weather); nunca exposta ao browser.
OPENWEATHER_API_KEY=
